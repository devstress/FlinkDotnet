# BackPressure Example

Run the integration tests (Aspire will start Kafka automatically):

```bash
dotnet test BackPressure.IntegrationTests --configuration Release --logger "trx;LogFileName=TestResults.trx"
```

