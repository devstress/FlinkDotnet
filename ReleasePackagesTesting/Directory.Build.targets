<Project>
  <!-- Override FormatCode target from root Directory.Build.targets -->
  <!-- Disable dotnet format for ReleasePackagesTesting projects to speed up builds -->
  <Target Name="FormatCode" />
</Project>
