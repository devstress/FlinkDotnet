<Project>
  <!-- Override FormatCode target from root Directory.Build.targets -->
  <!-- Disable dotnet format for FlinkIRRunner projects to speed up builds -->
  <Target Name="FormatCode" />
</Project>
